'use strict';

/**
 * @ngdoc filter
 * @name sigmaNgApp.filter:recursiveFilter
 * @function
 * @description
 * # recursiveFilter
 * Filter in the sigmaNgApp.
 */
angular.module('sigmaNgApp')
  .filter('recursiveFilter', ['$filter', function ($filter) {
    return function (input, childrenField) {
      
    };
  }]);
