/*
 * @ngdoc overview
 * @name Standards
 * @description
 * Konga gives you operations for many common features that you may need when developing an application (alerts, tab management, and some other handy stuff). 
 *
 * <b>You don't need to inject this service.</b>. To simplify from-view-access it's inherited via `$scope` to all controllers in your application.
 */ 

/*
 * @ngdoc object
 * @name Standards.Operations
 * @description
 * Konga gives you operations for many common features that you may need when developing an application (alerts, tab management, and some other handy stuff). 
 *
 * <b>You don't need to inject this service.</b>. To simplify from-view-access it's inherited via `$scope` to all controllers in your application.
 */ 

 /*
 * @ngdoc object
 * @name Standards.Permissions
 * @description
 * Konga gives you operations for many common features that you may need when developing an application (alerts, tab management, and some other handy stuff). 
 *
 * <b>You don't need to inject this service.</b>. To simplify from-view-access it's inherited via `$scope` to all controllers in your application.
 */ 

 /*
 * @ngdoc object
 * @name Standards.UI Components
 * @description
 * Konga gives you operations for many common features that you may need when developing an application (alerts, tab management, and some other handy stuff). 
 *
 * <b>You don't need to inject this service.</b>. To simplify from-view-access it's inherited via `$scope` to all controllers in your application.
 */ 